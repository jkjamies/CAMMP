package ${PACKAGE}

${DI_IMPORT}

import ${DOMAIN_FQN}.${CLASS_NAME}
${DATASOURCE_IMPORTS}

class ${CLASS_NAME}Impl(
    ${CONSTRUCTOR_PARAMS}
) : ${CLASS_NAME} {
    // TODO: implement repository methods
}
